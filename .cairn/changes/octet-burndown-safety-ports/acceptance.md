# Change-local acceptance

a[octet-burndown-safety-ports.zero] Pinned Octet root and `-p molten --lib` summaries report 0 `unbounded_channel` and 0 `usize_in_public_api`, and no other lint family count increases.
a[octet-burndown-safety-ports.bounds] A control channel admits exactly its capacity and denies one more without enqueueing it; a full timer inbox parks delivery and delivers every event once drained, without deadlock.
a[octet-burndown-safety-ports.no-suppression] No `allow`, baseline, quarantine, or `dylint.toml` change is introduced.
a[octet-burndown-safety-ports.behavior] Harness, fabric-simulation, and `fabricboundarycompat` outputs are byte-identical to the base; fmt, clippy with `-D warnings`, and the workspace tests pass.
