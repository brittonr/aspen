# Change-local acceptance

a[octet-burndown-size-parameters.zero] Pinned Octet root and `-p molten --lib` summaries report 0 `too_many_parameters`, and no other lint family count increases.
a[octet-burndown-size-parameters.no-rename] No public function, method, or type is renamed; only parameter shapes change, and every in-tree caller is migrated.
a[octet-burndown-size-parameters.no-suppression] No `allow`, baseline, quarantine, or `dylint.toml` change is introduced.
a[octet-burndown-size-parameters.behavior] Harness and fabric-simulation outputs are byte-identical to the base; fmt, clippy with `-D warnings`, the workspace tests, `fabricboundarycompat`, and the `fabric_execution::` tests pass.
