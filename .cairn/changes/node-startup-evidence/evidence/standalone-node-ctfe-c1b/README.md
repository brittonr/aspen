# Compile-time arithmetic correction: strict gate remains denied

Observed 2026-09-25 in the Molten producer `/home/brittonr/git/OnixResearch/molten-worktrees/node-content-service`, source/evidence HEAD `d0698cc0e919c98ee93f74fefb1db15ac43534a5`, with node source last changed at `4e2ef76db0fd66936fab160b206f481e3594012c`. The isolated Octet owner-review branch was **unpublished and unapproved at gate time** at `c1b0e0392aeebbafe1971deda6f4767f20b05fba`, while the remote still pointed to `c9c4f35997d5a1cf3dbc04a77221db321c982006`. After explicit confirmation, the exact c1b commit was published by non-force push to `git@github.com:OnixResearch/octet.git` branch `review/molten-octet-cohort-20260925`; `git ls-remote` verified the new ref. Publication is not owner approval or a release. Molten's pinned Octet input remains `c9b06bcf565c51d4a77d210e61b69ae51db9df25`. No pin update, source-scope reduction or lint-level change is claimed.

The owner `raw_arithmetic_overflow` classifier had identified compiler-evaluated `const BLAKE3_REF_CHAR_COUNT = PREFIX.len() + 64` as unchecked runtime arithmetic. A new paired fixture first reproduced two false findings in const/static initializers with the previously reviewed c9 object; its ordinary rustc compilation succeeded. The c1b correction recognizes const/static/associated-const initializers but stops before runtime stored-closure bodies. Ten direct rustc/driver probes pass: const, static, associated const, nested compile-time arithmetic, checked arithmetic, five existing runtime operations, a stored closure, a static lazy closure, depth-bounded runtime arithmetic, and an actual constant overflow error remain distinguished. The existing deep-constant control was updated to expect no diagnostic because the compiler evaluates it; a deep runtime counterpart still reports. The offline vendored Octet library suite passed **155/155** tests. Its first run found two documentation-summary drift failures; the table rows were restored to the canonical catalog text and the behavior notes moved into prose before the passing rerun.

The committed c1b library was built offline as the **only** scheduled Nix derivation, using the existing March-21 compiler, with no Stage0/compiler build: `/nix/store/5k9ar6nkzr2wlmys7zjc2fjv1n05fa7z-octet-0.1.0/lib/liboctet.so`, BLAKE3 `66de017bc646ef8efce7162f04efc25104394d7746abbf342202b405fd585456`. The installed CLI and driver remain the previously recorded binaries (BLAKE3 `dbf4b36ceacdcc8d372a48498ae3cb513692a22d4bff30774db20fb4e9295f4f` and `8e14cfcb3f0cfd993c5886e574408e11a49456dbb637b766e3d512e698d4474b`). The unmodified deny-all hook `/nix/store/9krx6k4dx3fvp5icdhcmznygh2fnkx31-source/hooks/octet-deny-all.sh --artifact-dir target/octet-review-c1b` used a symlink in ignored `target/octets-c1b/` to this exact library, `OCTET_PRECOMMIT_USE_INSTALLED=true`, the matching installed Dylint driver and toolchain, offline Cargo, a fresh `/home` target, and an absolute `/home` scratch path.

**Result: denied.** Hook exit **2**, Cargo exit **101**, `integration-failure`, **274 node-core errors**, zero warnings/autofixes. Host compiled without findings; runtime was not reached. Compared with the previous 275-finding c9 diagnostic on the *same producer source*, only the compile-time `raw_arithmetic_overflow` finding was removed. The remaining counts are 97 `path_segment_repetition`, 61 `assertion_density`, 51 `fragile_exhaustive_enum_match`, 13 `numeric_units`, 12 `ambiguous_params`, and 40 across nine other lints. The advisory owner rules, public API semantics, compiler-exhaustive enums, and bounded issue reporting were not falsified or suppressed to obtain a pass.

`command.txt`, `status.json`, `summary.txt`, and `provenance.jsonl` are byte-for-byte copies of the runner's `target/octet-review-c1b/` artifacts. Their respective BLAKE3 digests are `8c012a62966397220c02f04470a2e2250875784edc4d28fd604ed01b7a461804`, `04d0cbf6a6ca9735a53d782e1ab2b7734e4bd34407d6d5f0e4b00776fef6b3b9`, `8391f982e0943f853b2ac567ed4e7d6a585c0662ee426723cb76d826c077d258`, and `ac6283a1bf72371f6f96b40ebcd018e8bdd2e09b60371c575e0868b87d838d0a`. They establish a failed diagnostic at this exact scope, not an approved startup cohort.

The selected-source **518 tests across 15 suites** and the local debug binary's `init` success plus `run` denial/no-receipt result from `../standalone-node-reviewed-4e2/README.md` are reused only because the producer source and March-21 compiler are unchanged; the new Octet object is a diagnostic tool, not a changed node binary. No new normal-node run, VM/replay, independent compiler-file/generated-source inventory, or source-to-release-executable binding was performed or inferred. Both pending producer Cairn tasks and the consumer Molten Cairn remain open; Cua intake and Tile host are separate unresolved prerequisites.
