# Exact-source Nix preflight: evaluation only

Observed 2026-09-25 UTC in `/home/brittonr/git/OnixResearch/molten-worktrees/node-content-service`, clean producer HEAD `c28c2624538ac65faf32e796dc7cc537787441d8` (source commit `f64edd48750021092dac0032d32ccc3ba3d52036`). This is a local, unpublished producer checkpoint. No `flake.lock`, source selection, Octet pin, policy, or runtime admission changed.

The previously owner-recovered `executable-extent-src` cache at `/home/brittonr/.local/state/onix/molten-node-vm/sources/executable-extent.git` contains commit `025d9636f0161777710dac37b3c210ca0ad9483f`, tree `ee9ed56da3648372cddbf529e34e3db2eea938c3`. The corresponding exported directory's `nix hash path --type sha256 --sri` returned the locked NAR hash. More importantly, an offline `nix flake prefetch --json 'git+file:///home/brittonr/.local/state/onix/molten-node-vm/sources/executable-extent.git?rev=025d9636f0161777710dac37b3c210ca0ad9483f'` returned that same NAR. The recovery transport and earlier owner read are documented separately under `.cairn/changes/node-content-service/evidence/source-recovery.json`; no private source payload is copied into this evidence.

All four private inputs needed for `.#packages.x86_64-linux.molten-node.drvPath` were resolved **invocation-locally** from Git commits. Each offline `nix flake prefetch --json` succeeded, and its observed NAR matched the unchanged `flake.lock` entry:

| Input | Local Git source | Locked commit | Locked and observed NAR hash |
| --- | --- | --- | --- |
| `executable-extent-src` | `~/.local/state/onix/molten-node-vm/sources/executable-extent.git` | `025d9636f0161777710dac37b3c210ca0ad9483f` | `sha256-gN8jGxgxwckXu/YyG/p2YGkQnPrsJZPfkCy5t3r3yo4=` |
| `basalt-src` | `~/git/OnixResearch/basalt` | `89675cd4f585f837323c049e4a25f7b94c903038` | `sha256-lH3qGZ/MssqWR9cIJeyv8xcSO42IfVkvOHsn+O/ddwg=` |
| `cairn-src` | `~/.local/state/onix/molten-node-vm/sources/cairn-full.git` | `3b4c280b893f2709aebea21fc51a4f9eeba3fe3b` | `sha256-F1J1IE0gquK3y0F99MPNQGV9gxwNB8TGNKR9FEgB0nw=` |
| `kamacite-src` | `~/git/OnixResearch/kamacite` | `d76fe4abe543724d8fc0ac4b362187caf2e27622` | `sha256-Z6+dLkP2cziybjrAiRznP1cHUASyekhOwGl5ErWzZJc=` |

The existing Cairn checkout of Cairn was a partial/promisor clone with missing blobs, unsuitable for offline Nix Git import. A separate bare full clone was fetched via authenticated batch SSH into the isolated cache above. It did not alter the existing checkout or committed input. The Kamacite local checkout has no readable HEAD; Nix warned and chose `master`, but the explicit revision and observed NAR matched the lock. A local Git URL changes input transport/ref metadata; hash equality establishes fetched source bytes, **not** approval to edit the lock or change cohort policy.

The bounded preflight used `nix eval --offline --option min-free 0 --option max-free 0 --option builders '' --option max-jobs 0 --option substituters '' --option allow-import-from-derivation false --no-write-lock-file`, with four `--override-input <name> 'git+file:///<local-source>?rev=<locked-commit>'` arguments from the table, followed by `.#packages.x86_64-linux.molten-node.drvPath --raw`. Exit **0**, returning `/nix/store/sqfjrqni5kamffgfx0xsygy93gcln7ab-rust_molten-node-runtime-0.1.0.drv`. Nix warned that it was not writing the modified lock file; that is expected for the invocation-local transport overrides. `nix-store --query --requisites` on that derivation enumerated **3,571** store paths, including Stage0/bootstrap derivations.

With identical overrides and disabled builders/substituters/IFD, `nix build --dry-run --offline --no-link` exited **0** and reported **1,347 derivations will be built**. The plan begins with `tinycc-boot-mes` and contains the `bootstrap-stage0-stdenv-linux` toolchain chain. No realization or substituter request was launched: the available store does **not** provide an approved already-realized compatible toolchain for this graph, and Stage0 bootstrap is forbidden even for diagnostics. The earlier missing-Radicle-input blocker is narrowed to a toolchain/realization blocker for this preflight only.

Nix derivation evaluation and a dry-run are **not** a built executable, measured binary/source closure, strict Octet pass, approved startup receipt, VM run, native replay, or release proof. The unchanged pinned selected all-target gate still exits 2 at four host `missing_const_fn` false positives before checking node-core/runtime (see `../standalone-node-pinned-host-denial/README.md`). Normal startup and Molten VM transfer remain denied. No locked private URLs were re-fetched by Nix, no Stage0 build occurred, and no private source contents were published.
