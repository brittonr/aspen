# Pinned node-host source checkpoint: strict gate still denied

Producer cwd: `/home/brittonr/git/OnixResearch/molten-worktrees/node-content-service`; source revision `f64edd48750021092dac0032d32ccc3ba3d52036` on unpublished `feat/molten-node-content-service`. `Cargo.lock` SHA-256: `0b8d7a0dab775d0637067f0b8e656a3460ea6aaffa99e3f198ccde2f0c810860`. This is source and diagnostic evidence, **not** a successful Octet gate, an approved cohort, or a source-to-executable binding.

The producer's Octet pin remained `c9b06bcf565c51d4a77d210e61b69ae51db9df25`. The unchanged deny-all hook was `/nix/store/9krx6k4dx3fvp5icdhcmznygh2fnkx31-source/hooks/octet-deny-all.sh --artifact-dir target/octet`, invoked with the same installed pinned CLI (`dbf4b36ceacdcc8d372a48498ae3cb513692a22d4bff30774db20fb4e9295f4f`), pinned Dylint library (`10919bdd10b0049c1b1113f9757dc563441c6709b91efb02323ff5d32489447b`), March-21 Rust toolchain, driver, `OCTET_PRECOMMIT_USE_INSTALLED=true`, `CARGO_NET_OFFLINE=true`, `RUSTC_WRAPPER=''`, and `RUSTC_WORKSPACE_WRAPPER=''` as in `../standalone-node-exact-core-gate/README.md`. The selected all-target workspace-metadata package scope remained `-p molten-node-runtime -p molten-node-core -p molten-node-host --all-targets`. No source narrowing, lint allowance, severity change, baseline, macro concealment, visibility widening, or compiler bootstrap occurred.

The hook exited **2** (Cargo exit **101**): `integration-failure`, **4 errors**, zero warnings, zero autofixable. `status.json` and `summary.txt` here are byte-for-byte copies of the generated `target/octet/` artifacts with SHA-256 `7910a20f8587990f91526f816da32370f34fb904f6cb573612a8e96f0e983d76` and `6168297442107a7627ea3e55a4cc87fcdb7dde64d924bbc70e466265f44a7989`, respectively. The pinned source-text classifier still reports four non-const host functions as `missing_const_fn`:

| Site | Non-const operation that must be preserved |
| --- | --- |
| `error/mod.rs:49` | `std::fmt::Display::fmt` is a trait method; conditional `write!` produces observable diagnostic text. |
| `node/state/filesystem.rs:189` | `leaf_kind_optional` calls `Dir::symlink_metadata` in two live read/observation paths. |
| `node/state/locator.rs:114` | `validate_path_bytes` calls `Path::to_string_lossy` and creates an error after an overlong join, from two live callers. |
| `node/state/namespace.rs:156` | `validate_entry` enforces root `Arc::ptr_eq`, namespace and scope matching before both read and removal. |

The former 11 host findings are not a current result. Single-use append wrappers were removed, keeping local `len() >= 100_000` guards with checked failure count before each push. One-call path-prefix and validation wrappers were inlined without changing input denial. The 16 MiB state-file bound became its exact byte literal; bounded reads still check observed size, actual bytes, and no-follow acquisition. Irreducible shared I/O/provenance helpers remain intact. The four residual errors stopped compilation at `molten_node_host` before selected node-core or runtime could be checked; a fabricated `const fn`, suppressed lint, or deleted authority check cannot satisfy the contract.

After those source changes, with the existing non-bootstrap May-26 Rust toolchain and wrappers disabled, `cargo test --locked --offline -p molten-node-core -p molten-node-runtime -p molten-node-host -p molten-core` exited 0 (**919 passed across 20 suites**), `cargo test --locked --offline -p molten --lib` exited 0 (**1,361 passed**), `cargo test --locked --offline -p molten --test cliharness cli_cluster_` exited 0 (**four passed**), and `cargo clippy --locked --offline -p molten-node-runtime -p molten-node-core -p molten-node-host -p molten-core -p molten --all-targets -- -D warnings` exited 0. Targeted host `rustfmt --edition 2024 --check --config skip_children=true` exited 0; `git diff --check` exited 0 before source commit. The known inherited whole-workspace format check was not rerun or claimed.

`cargo build --locked --offline -p molten-node-runtime --bin molten-node` exited 0. The rebuilt binary initialized `/tmp/molten-node-pinned-source.X2gAgp/state` with `--node-id node:pinned-source-smoke`; `run --startup-out /tmp/molten-node-pinned-source.X2gAgp/startup.json` exited **1** with `node-startup-source-gate-required: real Octet evidence admission is not wired; synthetic startup evidence is forbidden`. An existence check confirmed no `startup.json`. No Nix package realization (the exact locked Radicle input remains unavailable), startup listener, VM transfer, native replay, or promotion is claimed. A correct owner-approved classifier and complete selected-source/source-binary evidence are still prerequisites; the pinned gate remains denied.

`cairn validate --root .` on this producer worktree also exited **1** before task validation: the installed Cairn binary rejected the committed `cairn-policy/generated/cairn-policy.json` because `cairn.workflow.profile.registry` lacks its required initial `outcome-machine` profile. The repository's `cairn-policy/contracts.ncl` explicitly allows only `spec-driven`, `assurance`, and `no-spec-delta`; no policy was changed to hide the mismatch. Producer Cairn validation is **not** claimed.
