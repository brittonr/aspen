# Exact node-core selected gate: denied

Producer source checkpoint: `510cd65c8247eb0ff112fea7d72b2d4f47ec71e2`, in `/home/brittonr/git/OnixResearch/molten-worktrees/node-content-service` on `feat/molten-node-content-service`. `Cargo.lock` SHA-256: `0b8d7a0dab775d0637067f0b8e656a3460ea6aaffa99e3f198ccde2f0c810860`. The selected strict package set in the committed workspace manifest is `-p molten-node-runtime -p molten-node-core -p molten-node-host --all-targets`; `molten-node-core` borrows the seven actual runtime-referenced pure module trees from `molten-core`. The broad legacy `molten-core` package remains for other consumers but is no longer a direct node build input. Source admission tests require the new owner manifest/lib and the borrowed source paths, reject the legacy broad-core build input, and check omissions. The two regenerated unit2nix plans use this lock identity; a unit graph is not a realized Nix package.

Exact strict hook command, with environment set on the same invocation:

```sh
PATH=/nix/store/1yvh3d6y3fj3xk2dgwczrp1dj5svd92c-rust-default-1.96.0-nightly-2026-03-21/bin:/tmp/molten-node-octet-pinned-c9-cli/bin:$PATH \
LD_LIBRARY_PATH=/nix/store/1yvh3d6y3fj3xk2dgwczrp1dj5svd92c-rust-default-1.96.0-nightly-2026-03-21/lib \
RUSTC_WRAPPER='' RUSTC_WORKSPACE_WRAPPER='' CARGO_NET_OFFLINE=true CARGO_BUILD_JOBS=2 \
TIGERSTYLE_TOOLCHAIN=nightly-2026-03-21-x86_64-unknown-linux-gnu \
TIGERSTYLE_DRIVER_PATH=/nix/store/r5bzbvda2ydnz09c6vxvqhxsmh37nhpw-dylint-driver-5.0.0/bin/dylint-driver \
TIGERSTYLE_LINT_LIB=/tmp/molten-node-octet-pinned-c9-lints/lib/liboctet@nightly-2026-03-21-x86_64-unknown-linux-gnu.so \
OCTET_PRECOMMIT_USE_INSTALLED=true \
sh /nix/store/9krx6k4dx3fvp5icdhcmznygh2fnkx31-source/hooks/octet-deny-all.sh --artifact-dir target/octet
```

Installed Octet CLI source revision `c9b06bcf565c51d4a77d210e61b69ae51db9df25`; BLAKE3 CLI `dbf4b36ceacdcc8d372a48498ae3cb513692a22d4bff30774db20fb4e9295f4f`, library `10919bdd10b0049c1b1113f9757dc563441c6709b91efb02323ff5d32489447b`, Dylint driver `8e14cfcb3f0cfd993c5886e574408e11a49456dbb637b766e3d512e698d4474b`. Rust compiler reports `rustc 1.96.0-nightly (ac7f9ec7d 2026-03-20)`. No Stage0/bootstrap, altered lint severity, source-selection narrowing, allowance, or baseline. The source checkpoint predates only this evidence record; it fixes the Rust/manifests/docs used by the check.

Hook exited **2** (`cargo_process_exit` 101) with `integration-failure`: **11 errors**, zero warnings, zero autofixable. Raw `status.json` and complete `summary.txt` here have exactly the SHA-256 digests of the generated `target/octet` files: `493f0040d249ed335063dcbab50f20673d9a2a136e4d012f103a6759d0ad04b3` and `2dbe8ecd755364778bac5722a01e26e5727f658cca6f5947525335b0f59c39b3` respectively. All 11 reported findings are in `molten_node_host` (eight `missing_const_fn`, two `borrowed_argument_types`, one `raw_arithmetic_overflow`); compilation terminated before strict coverage of selected `molten-node-core` or `molten-node-runtime`. This is **not** a successful three-package strict gate or matching source/build/binary approval.

The pinned Octet spec requires `missing_const_fn` not to flag trait methods, filesystem effects, or non-const calls (`specs/purity-effect-lints/spec.md`); its text classifier in `src/purity/effect_lints.rs::function_is_const_candidate` receives only the source snippet and nevertheless flags `std::fmt::Display::fmt` and node-state filesystem reads. `borrowed_argument_types` proposes `&mut [StoredEntry]` for `push_bounded_entry`, but slices cannot append. Its overflow finding is a compile-time `16 * 1_024 * 1_024` constant. Those concrete diagnostics must not be 'fixed' by an impossible `const fn`, removing bounded append, allowing/waiving the lint, or faking source evidence. Exact selected gate needs an owner-reviewed correct Octet classifier at an approved pin and a fresh full run; other selected packages have no strict coverage yet.

Separate local, non-promotion verification at this checkpoint: offline `cargo test --locked` passed 906 selected/core tests (20 suites), 1,361 root-library tests, and four `cli_cluster_` root integration tests. `cargo clippy --locked --offline -p molten-node-runtime -p molten-node-core -p molten-node-host -p molten-core -p molten --all-targets -- -D warnings` passed. `cargo build --locked --offline -p molten-node-runtime --bin molten-node` passed; rebuilt `molten-node init` wrote an isolated local identity, and `run` exited 1 with `node-startup-source-gate-required: real Octet evidence admission is not wired; synthetic startup evidence is forbidden`, with no startup receipt. Static `nix-instantiate --parse flake.nix` and new-module `rustfmt --edition 2024 --check --config skip_children=true` passed. An earlier Nix package evaluation was blocked by missing locked `executable-extent-src` Radicle input and HTTP 500 on its public seed; no Nix realization, production listener, VM, approval, or startup claim exists. The producer Cairn remains open.
