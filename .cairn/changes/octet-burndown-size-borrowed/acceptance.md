# Change-local acceptance

a[octet-burndown-size-borrowed.zero] Pinned Octet root and `-p molten --lib` summaries report 0 `borrowed_argument_types`, and no other lint family count increases.
a[octet-burndown-size-borrowed.no-rename] No public signature changes and nothing is renamed.
a[octet-burndown-size-borrowed.no-suppression] No `allow`, baseline, quarantine, or `dylint.toml` change is introduced.
a[octet-burndown-size-borrowed.behavior] Harness and fabric-simulation outputs are byte-identical to the base; fmt, clippy with `-D warnings`, the workspace tests, `fabricboundarycompat`, and the `fabric_execution::` tests pass.
