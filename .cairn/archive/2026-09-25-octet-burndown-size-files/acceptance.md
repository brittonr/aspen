# Change-local acceptance

a[octet-burndown-size-files.zero] Pinned Octet root and `-p molten --lib` summaries report 0 `excessive_file_length`, and no lint family count increases.
a[octet-burndown-size-files.verbatim] Inlining every include in the base and candidate trees reproduces each module's source, except the documented impl-header repetition, the `lib.rs` item hoist, the `Cargo.lock` run-time read, and the two source-scanning tests.
a[octet-burndown-size-files.tooling] Every tooling reference to a split file (ast-grep and flake scan lists, flake literal checks, tracey records, evidence-matrix targets, Cairn policy roots) covers the moved code.
a[octet-burndown-size-files.no-suppression] No `allow`, baseline, quarantine, or `dylint.toml` change is introduced, and no public item is renamed.
a[octet-burndown-size-files.behavior] Harness, fabric-simulation, and CLI-help outputs are byte-identical to the base; fmt, clippy with `-D warnings`, the workspace tests, `fabricboundarycompat`, and the `fabric_execution::` tests pass.
