# Evidence Gates Delta

## ADDED Requirements

### Requirement: Molten reproduces pinned Octet metadata hashing

r[molten.octet_fail_closed_ci.metadata_hash_parity] Molten MUST recompute Octet configuration and profile identities with the canonical field order and BLAKE3 algorithm defined by the pinned Octet metadata contract, independently from JSON map feature selection.

#### Scenario: Current Octet metadata is accepted as current

- GIVEN Octet metadata generated by pinned revision `fc38f59330b626961d166febfdf1a5aa6575460f`
- AND the checked `Cargo.toml` and `dylint.toml` bytes have not changed
- WHEN Molten recomputes configuration and profile identities
- THEN the identities match the Octet status metadata
- AND freshness checks do not report stale configuration.

#### Scenario: Changed metadata input remains stale

- GIVEN one checked metadata file has different bytes
- WHEN Molten recomputes the Octet configuration identity
- THEN the identity differs from the prior Octet status
- AND strict source-gate admission denies stale evidence.

#### Scenario: Octet parity does not weaken finding policy

- GIVEN configuration and profile identities match
- AND Octet status is warning-only or has unreviewed critical findings
- WHEN the strict gate evaluates the run
- THEN it still emits a deny receipt.
