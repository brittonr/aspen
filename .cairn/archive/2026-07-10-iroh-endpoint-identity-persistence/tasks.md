## Tasks

- [x] [serial] r[molten.node_identity.iroh_secret_backend_model] Inventory current node startup identity inputs, state-root layout, profile fields, and Iroh endpoint key handling. Evidence: reviewed `src/node/parts/identity/`, `src/node/parts/daemon/p017/body.rs`, and README node-control notes before implementation.
- [x] [serial] r[molten.node_identity.iroh_identity_resolution_core] Define a pure resolution core for explicit key, secret backend, persisted file, generate-and-persist, rotation, and fail-closed denial decisions. Evidence: added `resolve_iroh_secret_source` and `admit_iroh_endpoint_observation` with unit coverage.
- [x] [parallel] r[molten.node_identity.iroh_identity_permissions] Wire shell-owned secret file/backend handling with owner-only permissions where supported and deterministic diagnostics where unsupported. Evidence: generated secrets use owner-only mode on Unix, persisted-file permission facts are checked before read, and receipts include permission status.
- [x] [parallel] r[molten.node_identity.iroh_identity_drift_recovery] Add drift detection and admitted rotation/recovery receipt requirements for changed endpoint public keys under the same node scope. Evidence: drift now requires a matching `admitted_rotation_receipt_ref`; stale or missing rotation evidence denies.
- [x] [parallel] r[molten.node_identity.iroh_identity_receipt_redaction] Ensure startup, replay, peer-bootstrap, and rotation receipts bind public identity refs without exposing private key bytes, bearer tokens, raw tickets, or sensitive paths. Evidence: receipts bind public endpoint ids, backend/source refs, permission status, and rotation refs; tests assert secret material is absent.
- [x] [serial] r[molten.testing.iroh_identity_positive_negative_fixtures] Add positive fixtures for first boot, restart stability, explicit-key precedence, secret-backend load, and admitted rotation plus negative fixtures for malformed keys, unsafe permissions, drift without rotation, stale rotation, secret leakage, and endpoint-id-as-authority. Evidence: added positive/negative fixtures in `src/node/parts/identity/p002/body.rs`.
- [x] [serial] r[molten.testing.iroh_identity_positive_negative_fixtures] Update operator docs and run focused identity/startup tests plus Cairn validation. Evidence: updated README Iroh endpoint identity note; ran `cargo test --lib identity -- --nocapture`, `cargo test --lib peer -- --nocapture`, and `cargo fmt --check`.