# Verified node-replication pilot evidence

Generated by:

```bash
nix build .#checks.x86_64-linux.verified-node-replication-pilot \
  --no-link --print-out-paths -L
```

The pilot admits exact upstream source `eb8e27244a4f1b7714defe55aa495c6173a1c330`, verifies its Verus gitlink is `d8f073f31d72a5df2113b4ab9c011c1a27b38abf`, checks both fixed-output source identities and MIT license, and runs the Octet-profile Verus `0.2026.04.03.21dfcd2` binary.

Without `-V new-mut-ref`, the verifier reports seven unsupported-feature diagnostics. With that required flag, it reaches an internal `var_local_id` failure in `src/exec/rwlock.rs`; the deterministic decision is therefore `blocked-verifier-internal-error`. The audit also preserves 33 `#[verus::trusted]` markers, nine external-body markers, and one `assume` site, including trusted top-level refinement theorems and public traits.

Runtime dependency promotion remains denied. This is a local multicore/NUMA data-structure compatibility pilot, not network replication, distributed consistency, consensus, performance evidence, whole-system proof, or release readiness.
