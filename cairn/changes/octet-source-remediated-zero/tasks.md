## Phase 1: First CLI shell split

- [x] [serial] r[molten.octet_source_remediated_zero.cli_octet_shell_split] Extract Octet command parsing and dispatch from `src/main.rs` into `src/cli/ops/octet.rs`.
- [x] [serial] r[molten.octet_source_remediated_zero.no_cli_semantic_drift] Run focused Rust validation after the Octet CLI split to confirm command parsing and dispatch still compile.

## Phase 2: Evidence refresh

- [x] [serial] r[molten.octet_source_remediated_zero.evidence_refresh] Refresh workspace/lib Octet artifacts, focused object corpus, strict gate, remediation plan, and release dogfood evidence after the source scope is ready for a checkpoint.

## Phase 3: Disabled lint family burn-down

- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Retention command parsing and dispatch from `src/main.rs` into `src/cli/workflow/retention.rs` as the next CLI shell burn-down slice.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Delivery command parsing and dispatch from `src/main.rs` into `src/cli/workflow/delivery.rs` as a smaller follow-up CLI shell burn-down slice.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Provenance command parsing and dispatch from `src/main.rs` into `src/cli/workflow/provenance.rs` as another focused CLI shell burn-down slice.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Protocol command parsing and dispatch from `src/main.rs` into `src/cli/workflow/protocol.rs` as the next focused CLI shell burn-down slice.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Job command parsing and dispatch from `src/main.rs` into `src/cli/workflow/job.rs` as the next CLI shell hotspot burn-down slice.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Secrets and Plugin command parsing and dispatch from `src/main.rs` into `src/cli/runtime/secrets.rs` and `src/cli/ops/plugin.rs` as a low-risk CLI shell burn-down slice.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Node command parsing and dispatch from `src/main.rs` into `src/cli/ops/node.rs` as the next top-level CLI shell hotspot burn-down slice.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Repro command parsing and dispatch from `src/main.rs` into `src/cli/runtime/repro.rs` as the next test CLI shell hotspot burn-down slice.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Catalog command parsing and dispatch from `src/main.rs` into `src/cli/core/catalog.rs` as the next catalog/MCP CLI shell hotspot burn-down slice.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Chunk command parsing and dispatch from `src/main.rs` into `src/cli/core/chunk.rs` as the next chunk-store CLI shell hotspot burn-down slice.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Cache command parsing and dispatch from `src/main.rs` into `src/cli/core/cache.rs` as the next eval-cache CLI shell hotspot burn-down slice.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Artifact command parsing and dispatch from `src/main.rs` into `src/cli/core/artifact.rs` as the next artifact-registry CLI shell hotspot burn-down slice.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Storage command parsing and dispatch from `src/main.rs` into `src/cli/core/storage.rs` as the next typed-storage CLI shell hotspot burn-down slice.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Schema command parsing and dispatch from `src/main.rs` into `src/cli/core/schema.rs` as the next schema-identity CLI shell hotspot burn-down slice.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Upgrade command parsing and dispatch from `src/main.rs` into `src/cli/runtime/upgrade.rs` as the next upgrade-session CLI shell hotspot burn-down slice.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Transcript command parsing and dispatch from `src/main.rs` into `src/cli/core/transcript.rs` as the next transcript/replay CLI shell hotspot burn-down slice.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Rewrite command parsing and dispatch from `src/main.rs` into `src/cli/runtime/rewrite.rs` as the next structured-rewrite CLI shell hotspot burn-down slice.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Remote command parsing and dispatch from `src/main.rs` into `src/cli/workflow/remote.rs` as the next remote-dataspace CLI shell hotspot split.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Ledger and Chain command parsing and dispatch from `src/main.rs` into `src/cli/ops/ledger.rs` as the next evidence-ledger CLI shell hotspot split.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Receipts command parsing and dispatch from `src/main.rs` into `src/cli/evidence/receipts.rs` as the next operator-receipt CLI shell hotspot split.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Service command parsing and dispatch from `src/main.rs` into `src/cli/runtime/service.rs` as the next service-runtime CLI shell hotspot split.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Vat command parsing and dispatch from `src/main.rs` into `src/cli/runtime/vat.rs` as the next runtime-vat CLI shell hotspot split.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Coordination command parsing and dispatch from `src/main.rs` into `src/cli/workflow/coordination.rs` as the next coordination CLI shell hotspot split.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Dogfood command parsing and dispatch from `src/main.rs` into `src/cli/ops/dogfood.rs` as the next operator-dogfood CLI shell hotspot split.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Raft command parsing and dispatch from `src/main.rs` into `src/cli/runtime/raft.rs` as the next control-plane CLI shell hotspot split.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract replay-fixture command parsing and dispatch from `src/main.rs` into `src/cli/test/replayfixture.rs` as the next deterministic-replay CLI shell hotspot split.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Report command parsing and dispatch from `src/main.rs` into `src/cli/evidence/report.rs` as the next report validation/show CLI shell hotspot split.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract Gate command parsing and dispatch from `src/main.rs` into `src/cli/evidence/gate.rs` as the next gate-check CLI shell hotspot split.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract test Receipt command parsing and dispatch from `src/main.rs` into `src/cli/evidence/receipts.rs` as the next signed-receipt CLI shell hotspot split.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Extract harness Run/Replay command handling and failure receipt IO from `src/main.rs` into `src/cli/test/harness.rs` as the next harness CLI shell hotspot split.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Relocate Gate, Harness, Receipts, replay-fixture, and Report CLI shells under `src/cli/` to narrow root module-count and underscore-filename pressure while preserving module names and command semantics.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Relocate the remaining CLI shell modules under bounded `src/cli/{core,workflow,ops,runtime,evidence,test}/` groups to further narrow root module-count and underscore-filename pressure without changing command semantics.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Relocate underscore-named source and integration-test files into directory/file layouts such as `src/node/runtime.rs`, `src/octet/gate.rs`, `src/job/dag.rs`, and `tests/cliharness.rs`, then remove `underscore_in_module_filename` from `dylint.toml` while preserving Rust module names and public behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Relocate broad flat modules into directory `mod.rs` layouts such as `src/artifacts/mod.rs`, `src/ledger/mod.rs`, `src/resources/mod.rs`, and `src/runtime/envelope/mod.rs` so Molten source is clear of `module_file_count` findings; residual entries are external registry/rustlib paths pending Octet/config/tooling support.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split low-risk file-length hotspots into child files (`src/runtime/envelope/tests.rs`, `src/nixos/tests.rs`, `src/cli/workflow/coordination/bounded.rs`, and `src/cli/runtime/rewrite/input.rs`) so the no-disabled probe lowers `excessive_file_length` while preserving CLI/runtime behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split Octet baseline CLI handling and provenance CLI bounded input parsing into child files (`src/cli/ops/octet/baseline.rs` and `src/cli/workflow/provenance/input.rs`) so the no-disabled probe lowers `excessive_file_length` and `path_segment_repetition` while preserving command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split cache, chunk, catalog, and prod-soak CLI command enums plus small chunk/catalog helpers into child files (`src/cli/core/{cache,chunk,catalog}/command.rs`, `src/cli/core/chunk/io.rs`, `src/cli/core/catalog/filter.rs`, and `src/cli/ops/prodsoak/command.rs`) so the no-disabled probe lowers `excessive_file_length` to 117 while preserving command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split Dogfood command enums, release export archive helpers, and bounded signed-member loading into child files (`src/cli/ops/dogfood/{command.rs,archive.rs,signed.rs}`) so the no-disabled probe lowers `excessive_file_length` to 115 while preserving operator dogfood command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split receipts command enums and signed-keyring helper loading/summary code into child files (`src/cli/evidence/receipts/{command.rs,keyring.rs}`) so the no-disabled probe lowers `non_trait_imports` to 4593 while preserving receipt command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split the Repro command enum into `src/cli/runtime/repro/command.rs` so the no-disabled probe lowers `non_trait_imports` to 4591 while preserving repro command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split Repro bundle/unpack and local IO/failure receipt helpers into `src/cli/runtime/repro/{bundle.rs,io.rs}` so the no-disabled probe lowers `non_trait_imports` to 4539, `path_segment_repetition` to 3024, and `excessive_file_length` to 113 while preserving repro command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split Retention command payloads, dispatch handlers, live send/workflow glue, and local IO helpers into `src/cli/workflow/retention/{command,core,clearance,send,workflow,ops,io}.rs` and command child modules so the no-disabled probe lowers `non_trait_imports` to 4535, `function_length` to 434, and `excessive_file_length` to 111 while preserving retention command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split Job command payloads, dispatch handlers, sync/admission/execution helpers, worker local-run/schedule helpers, ref helpers, and local IO helpers into `src/cli/workflow/job/{command,core,io,refs,schedule,sync,worker}.rs` and child modules so the no-disabled probe lowers `path_segment_repetition` to 2992, `function_length` to 430, and `excessive_file_length` to 109 while preserving job command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split Node command payloads, dispatch handlers, lifecycle, authority, control ingress, health, workflow bundle/gate/apply/ack, and local IO helpers into `src/cli/ops/node/{command,core,lifecycle,authority,control,health,workflow}.rs` and child modules so the no-disabled probe lowers `path_segment_repetition` to 2986, `function_length` to 428, and `excessive_file_length` to 107 while preserving node command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split the remaining top-level `src/main.rs` Clap/root/test shell into `src/main/root.rs`, `src/main/root/command.rs`, `src/main/tests.rs`, and focused `src/main/tests/{core,job,ops}/` include files so the no-disabled probe lowers total findings to 8079 and `excessive_file_length` to 105 while preserving command and test behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split receipt dispatch/signing/key/operator/Preserves IO helpers into `src/cli/evidence/receipts/{io.rs,keys.rs,operator.rs,signing.rs}` so the no-disabled probe lowers total findings to 8031, `non_trait_imports` to 4496, `function_length` to 427, and `excessive_file_length` to 103 while preserving receipt command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split Cache command payloads, Preserves IO helpers, and dispatch handlers into `src/cli/core/cache/{command.rs,io.rs,ops.rs}` so the no-disabled probe lowers total findings to 8005, `non_trait_imports` to 4472, and `path_segment_repetition` to 2986 while preserving eval-cache command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split Artifact command payloads, Preserves IO helpers, and dispatch handlers into `src/cli/core/artifact/{command.rs,io.rs,ops.rs}` so the no-disabled probe lowers total findings to 7979, `non_trait_imports` to 4448, and `excessive_file_length` to 103 while preserving artifact command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split Schema command payloads, Preserves IO helpers, and dispatch handlers into `src/cli/core/schema/{command.rs,io.rs,ops.rs}` so the no-disabled probe lowers total findings to 7955, `non_trait_imports` to 4424, and `function_length` to 423 while preserving schema command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split Storage command payloads, Preserves IO helpers, and dispatch handlers into `src/cli/core/storage/{command.rs,io.rs,ops.rs}` so the no-disabled probe lowers total findings to 7929, `non_trait_imports` to 4400, and `function_length` to 421 while preserving typed-storage command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split Transcript command payloads, Preserves IO/failure helpers, and dispatch handlers into `src/cli/core/transcript/{command.rs,io.rs,ops.rs}` so the no-disabled probe lowers total findings to 7905, `non_trait_imports` to 4378, and `function_length` to 419 while preserving transcript command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split Chunk dispatch handlers and local byte/receipt helpers into `src/cli/core/chunk/ops.rs` so the no-disabled probe lowers total findings to 7891 and `non_trait_imports` to 4364 while preserving chunk-store command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split Catalog dispatch handlers and Preserves IO helpers into `src/cli/core/catalog/{io.rs,ops.rs}` so the no-disabled probe lowers total findings to 7871 and `non_trait_imports` to 4344 while preserving catalog/MCP command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split Provenance command payloads, Preserves IO helpers, and dispatch handlers into `src/cli/workflow/provenance/{command.rs,io.rs,ops.rs}` so the no-disabled probe lowers total findings to 7853 and `non_trait_imports` to 4324 while preserving provenance command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split Rewrite command payloads, Preserves IO helpers, and dispatch handlers into `src/cli/runtime/rewrite/{command.rs,io.rs,ops.rs}` so the no-disabled probe lowers total findings to 7831 and `non_trait_imports` to 4300 while preserving rewrite command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split Upgrade command payloads, Preserves IO helpers, and dispatch handlers into `src/cli/runtime/upgrade/{command.rs,io.rs,ops.rs}` so the no-disabled probe lowers total findings to 7809 and `non_trait_imports` to 4276 while preserving upgrade command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split Service command payloads, Preserves IO helpers, and dispatch handlers into `src/cli/runtime/service/{command.rs,io.rs,ops.rs}` so the no-disabled probe lowers total findings to 7789 and `non_trait_imports` to 4254 while preserving service command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split Report command payloads, summary selection, and failure-output IO into `src/cli/evidence/report/{command.rs,io.rs,ops.rs}` so the no-disabled probe lowers total findings to 7749 and `non_trait_imports` to 4212 while preserving report show/validate behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split Remote command payloads, Preserves IO helpers, and dispatch handlers into `src/cli/workflow/remote/{command.rs,io.rs,ops.rs}` so the no-disabled probe lowers total findings to 7731 and `non_trait_imports` to 4188 while preserving remote command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split Delivery command payloads, Preserves IO helpers, and dispatch handlers into `src/cli/workflow/delivery/{command.rs,io.rs,ops.rs}` so the no-disabled probe lowers total findings to 7713 and `non_trait_imports` to 4168 while preserving delivery command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split Coordination command payloads, Preserves IO helpers, and dispatch handlers into `src/cli/workflow/coordination/{command.rs,io.rs,ops.rs}` so the no-disabled probe lowers total findings to 7693 and `non_trait_imports` to 4146 while preserving coordination command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split Octet command payloads, Preserves IO helpers, and dispatch handlers into `src/cli/ops/octet/{command.rs,io.rs,ops.rs}` so the no-disabled probe lowers total findings to 7659 and `non_trait_imports` to 4124 while preserving Octet source-gate command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split Ledger and Chain command payloads, Preserves IO helpers, and dispatch handlers into `src/cli/ops/ledger/{command.rs,io.rs,ops.rs}` so the no-disabled probe lowers total findings to 7629 and `non_trait_imports` to 4090 while preserving ledger and chain exchange command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split NixOS VM command payloads, evidence file IO helpers, and dispatch handlers into `src/cli/ops/nixosvm/{command.rs,io.rs,ops.rs}` so the no-disabled probe lowers total findings to 7603, `non_trait_imports` to 4066, and `function_length` to 417 while preserving NixOS VM artifact command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split Raft, evidence gate, and protocol command payloads, evidence file IO helpers, and dispatch handlers into `src/cli/runtime/raft/{command.rs,io.rs,ops.rs}`, `src/cli/evidence/gate/{command.rs,io.rs,ops.rs}`, and `src/cli/workflow/protocol/{command.rs,io.rs,ops.rs}` so the no-disabled probe lowers total findings to 7529, `non_trait_imports` to 3996, and `path_segment_repetition` to 2996 while preserving CLI artifact command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Split Dogfood, ProdSoak, Plugin, Secrets, Vat, and replay-fixture command payloads, local IO helpers, and dispatch handlers into focused child modules so the no-disabled probe lowers total findings to 7413, `non_trait_imports` to 3884, and `function_length` to 413 while preserving command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Refine Cache, Catalog, Dogfood, and ProdSoak dispatch helpers so Dogfood local/release handlers live in child modules and the no-disabled probe lowers total findings to 7397 and `function_length` to 391 while preserving command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Move Repro command dispatch into `src/cli/runtime/repro/ops.rs` and reshape receipt key handlers into per-command helpers so the no-disabled probe lowers total findings to 7393 and `function_length` to 387 while preserving command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Reshape receipt operator handlers into per-command helpers so the no-disabled probe lowers total findings to 7391 and `function_length` to 385 while preserving command behavior.
- [x] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Move Repro bundle unpack and reveal-receipt helpers into `src/cli/runtime/repro/bundle/unpack.rs` so the no-disabled probe lowers total findings to 7389 and `function_length` to 383 while preserving repro unpack behavior.
- [ ] [serial] r[molten.octet_source_remediated_zero.disabled_lint_burndown] Continue splitting file-length, function-length, import, and path-repetition hotspots, and resolve external `module_file_count` handling, until disabled lint families can be removed or narrowed with source-remediated-zero evidence.
