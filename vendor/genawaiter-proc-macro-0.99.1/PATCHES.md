# Local Aspen patches

- Replaced `proc-macro-error 0.4` with maintained `proc-macro-error2 2.x` and updated the import path.

Remove this vendor patch when upstream genawaiter uses a maintained proc-macro error helper or the bao-tree/iroh-blobs stack no longer selects genawaiter-proc-macro.
