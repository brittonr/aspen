# Local Aspen patches

- Replaced unmaintained `paste` with maintained `pastey` at the serializer macro call site.

Remove this vendor patch when upstream nickel-lang-core moves to a maintained token-pasting macro or Aspen upgrades to a parent release that no longer selects `paste`.
