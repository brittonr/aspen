# Local Aspen patches

- Replaced unmaintained `paste` with maintained `pastey` in the error macro implementation.

Remove this vendor patch when upstream datafusion-common moves to a maintained token-pasting macro or Aspen upgrades to a parent release that no longer selects `paste`.
