[package]
name = "aspen-jobs-worker-blob"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Blob processing worker for the Aspen distributed job system"

[dependencies]
aspen-jobs = { path = "../aspen-jobs" }
aspen-blob = { path = "../aspen-blob" }

async-trait = "0.1"
flate2 = "1.0"
hex = "0.4"
iroh-blobs = "0.97"
serde_json = "1.0"
tracing = "0.1"
zstd = "0.13"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(madsim)'] }
