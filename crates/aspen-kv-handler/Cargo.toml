[package]
name = "aspen-kv-handler"
version = "0.1.0"
edition = "2024"
description = "Key-Value RPC handler for Aspen distributed cluster"
license = "AGPL-3.0-or-later"

[dependencies]
# RPC core abstractions (RequestHandler trait, ClientProtocolContext)
aspen-rpc-core = { path = "../aspen-rpc-core" }

# Local crates
aspen-client-api = { workspace = true }
aspen-core = { path = "../aspen-core" }

# Core dependencies
async-trait = "0.1"
anyhow = "1.0"

[features]
default = []

[dev-dependencies]
aspen-testing = { workspace = true }
tokio = { version = "1.0", features = ["full", "test-util"] }
