[package]
name = "aspen-storage-types"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Storage types for Aspen distributed systems"

[dependencies]
redb = "2.0.0"
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"

[dev-dependencies]
postcard = { version = "1.0", features = ["alloc"] }
