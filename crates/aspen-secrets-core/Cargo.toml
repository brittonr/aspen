[package]
name = "aspen-secrets-core"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Portable secrets engine types, state, and limits for Aspen"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"

[features]
default = []
