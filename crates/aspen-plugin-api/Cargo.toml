[package]
name = "aspen-plugin-api"
version = "0.1.0"
edition = "2024"
description = "Shared types for Aspen WASM plugin system"
license = "AGPL-3.0-or-later"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
semver = "1"
