[package]
name = "aspen-runtime-core"
version = "0.1.0"
edition = "2024"
description = "Portable Aspen runtime host-loading model types"
license = "AGPL-3.0-or-later"
repository = "https://github.com/brittonr/aspen"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
