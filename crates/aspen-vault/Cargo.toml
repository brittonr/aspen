[package]
name = "aspen-vault"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Key validation for reserved prefixes in Aspen distributed systems"

[dependencies]
thiserror = "2.0"

[dev-dependencies]
