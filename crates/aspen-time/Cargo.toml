[package]
name = "aspen-time"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
repository = "https://github.com/brittonr/aspen"
description = "Time utilities for Aspen distributed systems"

[features]
default = []
# Enable SimulatedTimeProvider for deterministic simulation testing
simulation = []

[dependencies]
# No dependencies - pure std::time

[dev-dependencies]
