//! Compatibility re-exports for secrets resource limits.
//!
//! The portable constants are owned by `aspen-secrets-core`; this module keeps
//! historical `aspen_secrets::constants::*` paths working.

pub use aspen_secrets_core::constants::*;
