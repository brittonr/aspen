id: credential-env-var
language: Rust
severity: hint
message: Direct environment credential access is runtime-authority inventory evidence; keep secrets behind explicit policy gates.
metadata:
  posture: inventory
  surface: runtime-authority
  non_claim: structural-candidate-only
rule:
  any:
    - pattern: std::env::var($NAME)
    - pattern: std::env::var_os($NAME)
