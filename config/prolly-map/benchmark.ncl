let Contracts = import "benchmark-contracts.ncl" in
let EntryCount = 64 in
let MutationCount = 1 in
let MinimumReusedBlocks = 1 in
let MaximumDiffRecords = 4 in
let MaximumRetainedBytes = 1048576 in
let MaximumGcCandidates = 4096 in
let MaximumRestartMismatches = 0 in
let MaximumAdversarialNodeBytes = 4096 in
let ObservedLogicalBytes = 4608 in
let ObservedBlockCount = 6 in
let ObservedBlockBytes = 5993 in
let ObservedReusedBlocks = 4 in
let ObservedDiffRecords = 1 in
let ObservedSkippedEqualNodes = 4 in
let ObservedGcCandidates = 2 in
let CredibleConsumers = 1 in
let RequiredConsumersForExtraction = 2 in
{
  schema = "molten.prolly-map-benchmark.v1",
  profile_ref = "blake3:820d0424eac0ce727d80750e485dcaac320137baa5b4ef21d0d67708ca8a41d5",
  profile_name = "logical-bounded-rebuild-first-v1",
  entry_count = EntryCount,
  mutation_count = MutationCount,
  sharing_min_reused_blocks = MinimumReusedBlocks,
  diff_max_records = MaximumDiffRecords,
  retained_bytes_maximum = MaximumRetainedBytes,
  gc_candidates_maximum = MaximumGcCandidates,
  restart_mismatch_maximum = MaximumRestartMismatches,
  adversarial_node_bytes_maximum = MaximumAdversarialNodeBytes,
  observed_logical_bytes = ObservedLogicalBytes,
  observed_block_count = ObservedBlockCount,
  observed_block_bytes = ObservedBlockBytes,
  observed_reused_blocks = ObservedReusedBlocks,
  observed_diff_records = ObservedDiffRecords,
  observed_skipped_equal_nodes = ObservedSkippedEqualNodes,
  observed_gc_candidates = ObservedGcCandidates,
  observed_restart_verified = true,
  credible_consumers = CredibleConsumers,
  extraction_required_consumers = RequiredConsumersForExtraction,
  extraction_disposition = "retain-current",
  raw_timings_are_correctness = false,
  creates_repository = false,
  approves_dependency = false,
} | Contracts.Benchmark
