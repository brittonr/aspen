let Contracts = import "contracts.ncl" in
let FormatVersion = 1 in
let MinimumNodeBytes = 256 in
let TargetNodeBytes = 1024 in
let MaximumNodeBytes = 4096 in
let MinimumFanout = 2 in
let TargetFanout = 4 in
let MaximumFanout = 8 in
let MaximumKeyBytes = 64 in
let MaximumValueBytes = 1024 in
let MaximumEntries = 4096 in
let MaximumTreeHeight = 16 in
let MaximumDiffRecords = 8192 in
let MaximumGraphFacts = 16384 in
{
  schema = "molten.prolly-map-profile.v1",
  format = "prolly-semantic-map-v1",
  format_version = FormatVersion,
  profile_ref = "blake3:820d0424eac0ce727d80750e485dcaac320137baa5b4ef21d0d67708ca8a41d5",
  key_codec = "canonical-bytes-v1",
  value_codec = "canonical-bytes-v1",
  comparison = "unsigned-lexicographic-v1",
  node_codec = "molten-prolly-node-binary-v1",
  boundary_domain = "molten-prolly-boundary:v1",
  boundary_seed_ref = "blake3:4da8a4af528826c160a956f28ad0b8cce119c2636ea8a94b48b99e6596805261",
  size_accounting = "exact-encoded-bytes-v1",
  min_node_bytes = MinimumNodeBytes,
  target_node_bytes = TargetNodeBytes,
  max_node_bytes = MaximumNodeBytes,
  min_fanout = MinimumFanout,
  target_fanout = TargetFanout,
  max_fanout = MaximumFanout,
  profile_domain = "molten-prolly-profile:v1",
  leaf_domain = "molten-prolly-leaf:v1",
  internal_domain = "molten-prolly-internal:v1",
  root_domain = "molten-prolly-root:v1",
  limits = {
    max_key_bytes = MaximumKeyBytes,
    max_value_bytes = MaximumValueBytes,
    max_entries = MaximumEntries,
    max_tree_height = MaximumTreeHeight,
    max_diff_records = MaximumDiffRecords,
    max_graph_facts = MaximumGraphFacts,
  },
  claims_collision_impossibility = false,
  claims_database_correctness = false,
  owns_branch_authority = false,
  owns_effect_release = false,
  owns_gc_deletion = false,
  extraction_approved = false,
} | Contracts.Profile
