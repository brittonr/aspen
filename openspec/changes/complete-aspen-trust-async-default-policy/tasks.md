# Tasks

- [x] Gate `aspen-trust` async service modules and optional dependencies behind an explicit `async` feature.
- [x] Preserve runtime compatibility by enabling `aspen-trust/async` from `aspen-raft` trust feature.
- [x] Add `aspen_trust` to trust/crypto/secrets policy and readiness checker real package coverage.
- [x] Update extraction docs/specs with the async/default policy and remaining serialization blocker.
- [x] Capture cargo, dependency, readiness, OpenSpec, markdown, and diff evidence.
