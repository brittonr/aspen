# Tasks

- [x] Add `SecretsMountProvider` trait and `MountRegistry` implementation.
- [x] Re-export the provider trait from `aspen-secrets`.
- [x] Update `aspen-secrets-handler::SecretsService` to depend on the provider trait instead of concrete `MountRegistry`.
- [x] Run focused cargo checks/tests and node compatibility check.
- [x] Capture dependency/source guards and OpenSpec/markdown evidence.
- [x] Archive the completed boundary change.
