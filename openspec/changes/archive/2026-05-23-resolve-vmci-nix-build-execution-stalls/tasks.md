## Phase 1: Trace and Contract

- [x] [serial] Trace the live `ci_nix_build` execution path from VM worker job receipt through payload transform, command request construction, command execution, timeout handling, and worker job completion publication.
- [x] [serial] Add a shared phase marker vocabulary for `ci_nix_build` covering payload decode, workspace ready, Nix payload transform, command request built, command execute enter, command start, heartbeat, timeout, output drain, execute return, result publish enter, and result published. (VM worker job-spec parse/pre-executor markers, Nix payload transform, working-dir rewrite, local job construction, executor-enter, local executor payload/workspace/request/execute-enter/execute-return markers, command timeout markers, and worker result publish enter/published markers are now retained in diagnostics.)
- [x] [parallel] Add redaction tests proving markers and dogfood timeout summaries omit raw tickets, secret keys, credential-like env values, and unbounded command arguments. (Phase marker/request-shape tests prove marker output omits command args; dogfood running-job timeout summary redacts tickets, secret-key flags, cluster-ticket flags, and password-like flag values; VMCI diagnostic evidence tests preserve redaction before cleanup.)

## Phase 2: Timeout and Finalization Semantics

- [x] [depends:trace] Ensure `ci_nix_build` uses the same bounded command runner/watchdog/output-drain timeout semantics as shell commands, or extract a shared helper used by both paths.
- [x] [depends:trace] Add focused tests for Nix payload timeout propagation into the executable command request.
- [x] [depends:trace] Add tests proving an over-timeout `ci_nix_build` returns a failed execution/job result and cannot remain `running` solely because stdout/stderr readers, command descendants, or log channels stay open. (No-VM fake-`nix` job proves over-timeout Nix-shaped payload returns `JobResult::Failure` and unregisters the executor handle; existing output-drain tests cover stuck stdout/stderr readers.)
- [x] [parallel] Add diagnostics classification tests for pre-spawn Nix-build stall, command-started stall, command-timeout finalization, missing command progress, and job-result-published boundaries. (Dogfood VMCI diagnostics classify pre-executor progress, executor-enter, command execute return, workspace materialization timeout/failure, command progress, and result publish markers; timeout summary tests cover missing command progress.)

## Phase 3: Dogfood Evidence and Rail

- [x] [depends:timeout-finalization] Extend dogfood CI wait-timeout summaries with bounded running-job metadata, latest phase marker, missing-marker classification, and CI log tail for `ci_nix_build` jobs.
- [x] [depends:timeout-finalization] Add a cheap VMCI Nix-build timeout/finalization rail or fixture that exercises the real Forge/RPC/source/blob/VM worker/CI job path with a deterministic short timeout. (Added `vm-ci-nix-timeout`/`vmci-nix-timeout` CLI profile and `dogfood-local-vmci-nix-timeout` app using the existing `checks.x86_64-linux.fmt` attr with a 5s job timeout so the uncommitted source snapshot can exercise `ci_nix_build` timeout/finalization.)
- [x] [depends:rail] Add Nix app/CLI selection tests and `nix eval` coverage for the new timeout/finalization rail. (Added dogfood rail mapping and CI override selection tests; `nix eval .#apps.x86_64-linux.dogfood-local-vmci-nix-timeout.program` passed.)

## Phase 4: Verification and Live Proof

- [x] [depends:rail] Run focused Rust tests for executor timeout finalization, Nix payload command construction, dogfood diagnostics, and redaction. (Initial no-VM slice: full `aspen-ci-executor-shell`, Nix transform test, marker/request-shape tests.)
- [x] [depends:rail] Run OpenSpec validation for this change and related VMCI harness/debug changes. (Initial no-VM slice: this change validates strictly.)
- [x] [depends:rail] Run the new VMCI Nix-build timeout/finalization rail and record the receipt path plus final status. (Latest proof: `nix run .#dogfood-local-vmci-nix-timeout`; receipt `/home/brittonr/.cargo-target/aspen-dogfood-vmci-receipts/dogfood-20260519T150149Z.json`; diagnostics `/home/brittonr/git/aspen/target/runtime-proof/vmci-diagnostics/dogfood-20260519T150149Z/`; final `build:executor_command`/`executor_timeout`, not `ci_wait_timeout`; diagnostics boundary `job_result_published` with retained `command_timeout`, `command_execute_returned`, `result_publish_enter`, and `result_published` markers.)
- [x] [depends:live-timeout-rail] Re-run `nix run .#dogfood-local-vmci-medium` and record whether `build-cli` publishes success/failure or produces a more specific phase-classified failure instead of silent `running` at dogfood timeout. (Initial medium receipt `/home/brittonr/.cargo-target/aspen-dogfood-vmci-receipts/dogfood-20260520T162316Z.json` published final FD-pressure failure; superseding fix change is `avoid-vmci-nix-store-fd-pressure`. Later local-store/UCAN/rail-budget runs removed FD/UCAN/GitHub failures but exposed a cold guest-local-store timeout/misclassification gap: latest receipt `/home/brittonr/.cargo-target/aspen-dogfood-vmci-receipts/dogfood-20260521T003611Z.json` failed `build-cli` with `executor_timeout` after 805s despite `timeout_secs=3600`; follow-up patch adds cache-warm split and timeout-origin `elapsed_secs`/`origin` markers.)

## Phase 5: Medium Hidden Timeout Classifier and Lease Evidence

- [x] [depends:live-timeout-rail] Record the cache-warm medium failure that reported `executor_timeout` without any timeout-origin marker. (Receipt `/home/brittonr/.cargo-target/aspen-dogfood-vmci-receipts/dogfood-20260521T143659Z.json`; `format-check` passed; `cache-warm/build-cli-deps` failed after about 765s despite `timeout_secs=2400`; no `command_timeout` or `executor_job_timeout` marker.)
- [x] [serial] Patch dogfood failure classification so executor timeout requires timeout-origin markers and active command progress without timeout-origin is classified as `executor_command_failed`.
- [x] [serial] Preserve bounded failure-log tails and add queue visibility extension acceptance/rejection markers for the next live run.
- [x] [depends:hidden-timeout-classifier] Run targeted validation and the cheap VMCI timeout rail before another medium rerun. Verified `nix run .#rustfmt`; `cargo test -p aspen-dogfood vmci -- --nocapture`; `cargo test -p aspen-ci-executor-shell timeout -- --nocapture`; `openspec validate resolve-vmci-nix-build-execution-stalls --strict --json`; and `nix run .#dogfood-local-vmci-nix-timeout`. Timeout rail returned expected failed job classification with receipt `/home/brittonr/.cargo-target/aspen-dogfood-vmci-receipts/dogfood-20260521T200715Z.json`: `build:executor_command` / `executor_timeout`, command timeout markers included `elapsed_secs=5` and `origin=guard`/`origin=select`, and diagnostics handle `target/runtime-proof/vmci-diagnostics/dogfood-20260521T200715Z`.
- [x] [depends:hidden-timeout-classifier] Re-run VMCI medium after cache-warm split, timeout-origin markers, and guest-local Nix store propagation to shell jobs. Successful proof: `nix run .#dogfood-local-vmci-medium` via session `proc_610989a9d21b`, log `target/runtime-proof/vmci-medium-20260523T001418Z.log`, receipt `/home/brittonr/.cargo-target/aspen-dogfood-vmci-receipts/dogfood-20260523T002711Z.json`, CI run `ef06231a-3b09-4ac0-a749-7272ad97014b`; `format-check`, `build-cli-deps`, and `build-cli` all completed successfully.
