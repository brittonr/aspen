# Tasks

- [x] Gate `aspen-crypto` identity lifecycle helpers behind an explicit `identity`
  feature.
- [x] Replace concrete `iroh` dependency with `iroh-base` key types for identity
  consumers.
- [x] Preserve `aspen-secrets::identity` compatibility re-exports via
  `aspen-crypto/identity`.
- [x] Fix `aspen-secrets` no-default test mocks for the split KV capability traits.
- [x] Capture downstream metadata, negative boundary, compatibility, and readiness
  evidence.
- [x] Update trust/crypto/secrets crate-extraction docs and inventory to mark the
  first blocker complete.
