Compatibility evidence for trust-crypto-secrets

- `cargo test -p aspen-trust i12_ -- --nocapture` passes property-style pure trust checks.
- Malformed share/key boundary checks pass for zero share coordinate, duplicate share coordinate, and corrupted share digest.
- `cargo check -p aspen-secrets-handler` passes, proving runtime secrets handler compatibility remains intact while the reusable trust boundary is documented.

See `i12-trust-crypto-secrets-tests.txt` for the full command transcript.
