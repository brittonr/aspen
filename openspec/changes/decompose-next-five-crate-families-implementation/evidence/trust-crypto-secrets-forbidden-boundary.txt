Forbidden boundary evidence for trust-crypto-secrets

Reusable defaults must not pull Raft, Redb, secrets-service handlers, node bootstrap, concrete Iroh endpoint construction, SOPS file IO, client transport, or ambient runtime shells.

I11 inventory evidence shows `aspen-trust` checks/trees without Aspen Raft, Redb, handlers, node bootstrap, or secrets-service runtime shells. I12 tests exercise pure Shamir/share/digest behavior in `aspen-trust`; compatibility checks keep `aspen-secrets-handler` outside the reusable default.

Boundary risk intentionally recorded for follow-up: `aspen-crypto` still includes default Iroh/tokio filesystem lifecycle helpers, so only transport-free helpers are reusable candidates until narrowed.
