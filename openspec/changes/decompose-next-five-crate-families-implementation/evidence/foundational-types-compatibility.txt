family: foundational-types
task: I4
status: pass

Compatibility checked:
- `aspen-core::storage::KvEntry` remains available to no-default portable consumers.
- `aspen-core-shell` keeps the shell-facing compatibility alias: the `shell-alias-smoke` fixture depends on package `aspen-core-shell` as crate name `aspen-core` and compiles `aspen_core::storage::SM_KV_TABLE`.

Evidence:
- openspec/changes/decompose-next-five-crate-families-implementation/evidence/i4-storage-types-redb-boundary.txt

I5 compatibility checked:
- `cargo test -p aspen-traits` passes 11 trait capability tests.
- `cargo check -p aspen-core --no-default-features` preserves the core re-export path.
- `cargo check -p aspen-raft-kv` preserves a representative runtime consumer of the composite `KeyValueStore` compatibility trait.

Evidence:
- openspec/changes/decompose-next-five-crate-families-implementation/evidence/i5-aspen-traits-positive.txt
