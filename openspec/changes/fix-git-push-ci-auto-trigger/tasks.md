## Tasks

- [ ] 1. Add `announce_ref_update` calls to `handle_git_bridge_push` in `git_bridge.rs` after successful ref updates
- [ ] 2. Set `ASPEN_CI_FEDERATION_CI_ENABLED=true` for bob's cluster in `dogfood-federation.sh`
- [ ] 3. Add integration test verifying git push fires RefUpdate announcement
- [ ] 4. Build and run tests
