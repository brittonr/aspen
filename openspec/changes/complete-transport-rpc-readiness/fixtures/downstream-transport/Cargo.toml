[package]
name = "downstream-transport"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"

[workspace]

[dependencies]
aspen-transport = { path = "../../../../../crates/aspen-transport", default-features = false }
