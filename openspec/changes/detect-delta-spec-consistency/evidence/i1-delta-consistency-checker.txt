$ python3 scripts/check-openspec-delta-consistency.py detect-delta-spec-consistency
OK: openspec/changes/detect-delta-spec-consistency delta specs are consistent

$ python3 scripts/check-openspec-delta-consistency.py --help | sed -n 1,8p
usage: check-openspec-delta-consistency.py [-h] [--repo-root REPO_ROOT]
                                           [--warnings-as-errors]
                                           change

Check OpenSpec delta specs for structural consistency. This is intentionally
dependency-light so preflight and fixture tests can run in minimal repos. It
validates the currently relevant invariants: * every added requirement and
added scenario has an ID line; * every modified/removed requirement targets an
