{
  "candidate_family": "redb-raft-kv",
  "passed": false,
  "failures": [
    "aspen_kv_types: exception `aspen-kv-types -> aspen-constants` has unassigned owner",
    "aspen_raft_compat: exception `aspen-raft -> aspen-core-shell` has unassigned owner",
    "aspen_raft_compat: exception `aspen-raft -> iroh` has unassigned owner",
    "aspen_raft_kv: exception `aspen-raft-kv -> openraft` has unassigned owner",
    "aspen_raft_kv_types: exception `aspen-raft-kv-types -> openraft` has unassigned owner",
    "aspen_raft_network: exception `aspen-raft-network -> iroh` has unassigned owner",
    "aspen_raft_network: exception `aspen-raft-network -> irpc` has unassigned owner",
    "aspen_redb_storage: exception `aspen-redb-storage -> redb` has unassigned owner",
    "aspen_redb_storage: exception `aspen-redb-storage -> openraft` has unassigned owner"
  ],
  "warnings": [
    "aspen_raft_kv: package does not exist yet; direct dependency check deferred"
  ],
  "checked_candidates": [
    "aspen_kv_types",
    "aspen_raft_compat",
    "aspen_raft_kv",
    "aspen_raft_kv_types",
    "aspen_raft_network",
    "aspen_redb_storage"
  ]
}