# Test secrets file for kitty-secrets-test.sh
# This is a plaintext file (no SOPS encryption) for testing purposes.
# When SOPS metadata is absent, the secrets module treats the file as plaintext.

# Empty trusted roots - not using token verification in tests
trusted_roots = []

# No signing key needed for basic tests
# signing_key = ""

# No pre-built tokens needed
[tokens]

# Example secrets for testing
[secrets.strings]
test_api_key = "sk-test-12345"
test_password = "supersecret"

[secrets.bytes]
